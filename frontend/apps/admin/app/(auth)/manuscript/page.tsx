import React from 'react';
import { LayoutContent } from '@admin/(auth)/_components/layout-content';

const Page = () => {
	return <LayoutContent breadcrumb={[{ name: '投稿管理' }]}>Manuscript Page</LayoutContent>;
};

export default Page;
