import React from 'react';
import { LayoutContent } from '@admin/(auth)/_components/layout-content';

const Page = () => {
	return <LayoutContent breadcrumb={[{ name: 'マスタ管理' }]}>Master Page</LayoutContent>;
};

export default Page;
